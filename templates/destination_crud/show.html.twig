{% extends 'base.html.twig' %}

{% block title %}Destination{% endblock %}

{% block body %}
    <h1>Destination</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ destination.id }}</td>
            </tr>
            <tr>
                <th>NameDestination</th>
                <td>{{ destination.nameDestination }}</td>
            </tr>
            <tr>
                <th>PriceDestination</th>
                <td>{{ destination.priceDestination }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_destination_crud_index') }}">back to list</a>

    <a href="{{ path('app_destination_crud_edit', {'id': destination.id}) }}">edit</a>

    {{ include('destination_crud/_delete_form.html.twig') }}
{% endblock %}
