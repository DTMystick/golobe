{% extends 'base.html.twig' %}

{% block title %}Destination{% endblock %}

{% block body %}
<div class="row">
    <div class="col-2"></div>
    <div class="col-8">
    <h1>Informations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ destination.id }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ destination.nameDestination }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ destination.priceDestination }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_destination_crud_index') }}" class="btn btn-primary">Retour</a>

    <a href="{{ path('app_destination_crud_edit', {'id': destination.id}) }}" class="btn btn-primary">Modifier</a>

    {{ include('destination_crud/_delete_form.html.twig') }}
</div>
</div>
    <div class="col-2"></div>
{% endblock %}
