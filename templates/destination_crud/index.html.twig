{% extends 'base.html.twig' %}

{% block title %}Destination index{% endblock %}

{% block body %}
<div class="row">
    <div class="col-2"></div>
    <div class="col-8">
        <h1>Destination index</h1>
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>NameDestination</th>
                <th>PriceDestination</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
            {% for destination in destinations %}
            <tr>
                <td>{{ destination.id }}</td>
                <td>{{ destination.nameDestination }}</td>
                <td>{{ destination.priceDestination }}</td>
                <td>
                    <a href="{{ path('app_destination_crud_show', {'id': destination.id}) }}">show</a>
                    <a href="{{ path('app_destination_crud_edit', {'id': destination.id}) }}">edit</a>
                </td>
            </tr>
            {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
    <a href="{{ path('app_destination_crud_new') }}" class="btn btn-primary">Create new</a>
</div>
</div>
    <div class="col-2"></div>
    {% endblock %}
    